import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    borderRadius: 7,
    backgroundColor: '#dedede',
    margin: 5,
    fontWeight: 'bold',
    shadowColor: 'black',
    elevation: 2,
    
  },
});
